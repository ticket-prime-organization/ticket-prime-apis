namespace TicketPrimeauth.Domain.Enums;

public enum TicketType
{
    GeneralAdmission = 1, // Standard entry ticket
    VIP = 2,              // VIP ticket with premium perks
    EarlyBird = 3,        // Discounted ticket for early buyers
    Group = 4,            // Group ticket with bulk pricing
    ReservedSeating = 5   // Assigned seat ticket
}
